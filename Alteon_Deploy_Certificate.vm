#property('summary', 'Configuration template demo [Tue Dec 10 2024 09:38:16 GMT+0200 (Israel Standard Time)]')
#device($alteons, 'type=alteon[]')

#param($serialNewCert, 'string', 'in', 'prompt=Serial New Cert', 'required=false')
#param('output', 'object','out')

#param($name,'string','in', 'prompt=name')
#param($key, 'string', 'in', 'format=pem', 'prompt=Private key in PEM format')
#param($password, 'string', 'in', 'prompt=Key passphrase', 'required=false', 'defaultValue=', 'format=password')
#param($srvrcert, 'string', 'in', 'format=pem', 'prompt=Server Certificate in PEM format')
#param($intermca, 'string', 'in', 'format=pem', 'prompt=Intermediate CA Certificate in PEM format')

#set($expect.timeout = 15)
#set($expect.failOnTimeout = true)
#set($expect.failOnEOF = true)

#set($output="Unchanged")

#set($masterAlteons=[])

#foreach($alteon in $alteons)
    #select($alteon)
    #set($updateCert=1)
    #savereplies(false)
    
    	#if($serialNewCert) ## Meaning, if this is not the first time for deploying that cert
            #set($bean = $alteon.newBean('SlbNewSslCfgCertsEntry'))
            #set($bean.type = $alteon.enumValue('EnumSlbSslCertsType','SERVERCERTIFICATE'))
            #set($beans = $alteon.readAll($bean))
            #foreach ($bean in $beans)
                #log("Certificate name is - $bean.ID and the serial is - $bean.serial")
                #if($bean.serial == $serialNewCert)
                    #set($updateCert=0)
                    #log("Alteon $alteon has the updated cert - $name")
                    #break
                #end
            #end
        #end
        
        #if($updateCert == 1)
            #log("Alteon $alteon does not have this cert - $name")
		    /c/slb/ssl/certs/import key "$name" text

            #expectCase()

                #case('.*Overwrite\? \(y/n\)')
                    y
                    #expectContinue()
                #end

                #case('To abort type "..."\r?\n?>')
                #end
            #end

            #foreach($line in $key.split('\r?\n'))
                $alteon.sendLine($line)
                #expectCase()
                    #case('Enter key passphrase:')
                        #if(!$password.isEmpty())
                            $password
                        #else
                            $alteon.sendLine('')
                        #end
                    #end

                    #case('>')
                    #end
                    #case('Error:.*')
                        #error("The key import failed: ${expect[0].string}")
                    #end
                    #case('>>.+# ')
                    #end
                #end
            #end

            #expectCase()
                #case('Error:.*')
                    #error("The key import failed: ${expect[0].string}")
                #end

                #case('>>.+# ')
                #end
            #end


            #import_cert("certificate", $name, $srvrcert)
            #import_cert("intermca", $name, $intermca)

            /c/slb/ssl/certs/cert $name/intermca cert $name

            #ApplySaveSync($alteon)
            
            #if($output != "Success")
                #set($output = "Success")
            #end
        #end
    #end
#end


#macro(import_cert, $type, $certName, $certificate)
    #savereplies(false)

        /c/slb/ssl/certs/import $type "$certName" text

        #expectCase()

            #case('Overwrite\? \(y/n\)')
                y
                #expectContinue()
            #end

            #case('To abort type "..."\r?\n?>')
                ##break
            #end
        #end

        #foreach($line in $certificate.split('\r?\n'))
          $alteon.sendLine($line)
          #expectCase()
            #case('>')
            #end
            #case('Error:.*')
                  #error("The import failed: ${expect[0].string}")
            #end
            #case('>>.+# ')
            #end
            #case('intermca added.')
            #end
          #end
        #end
    #end
#end


#macro(ApplySaveSync, $device)
    #savereplies(false)
    	#select($device)
        apply
        #set($bean = $device.readBean('HaSwitchInfo'))
        #log("Device HA status is: $bean.haSwitchInfoState")
        
        #expectCase()
            #case('No apply needed.*')
            #log("No apply needed")
            #end
            #case('Synchronize configuration changes.*')
                #if("$bean.haSwitchInfoState" == "master")
                #log("Syncing")
				y
                #else
                #log("Not Syncing")
				n
                #end
                #expect('Apply complete.*')
            #end
            #case('Apply complete.*')
            #log("Apply complete")
            #end
            #case('.*Error.*')
                #error($expect.input.replace('apply',''))
            #end
        #end

        save
        #expectCase()
        	#case('No save needed.*')
        		#log("No save needed")
        	#end
        	#case('Confirm saving to FLASH.*')
        		#log("Saving")
				y
        	#end
        	#case('Please enter y to perform the action, n to skip it.*')
        		#log("Saving retry prompt")
				y
        	#end
        #end
    #end
#end
